name: flutter_bloc_2021
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_bloc: ^7.0.0
  equatable: ^2.0.0
  http: ^0.12.2

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:

  # the material Icons class.
  uses-material-design: true

